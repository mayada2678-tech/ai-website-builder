[project]
name = "ai-website-builder-webhook"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4",
    "fastmcp",
    "mcp",
    "openai",
    "pypdf",
    "requests",
    "stripe",
    "streamlit==1.62.0",
]

[tool.vercel]
entrypoint = "api.stripe_webhook:handler"
